import { Chain } from "fetcch-chain-data";
export declare const findAddress: (idStr: string, apiKey: string, chain?: string, fallbackToDefault?: boolean) => Promise<{
    address: string;
    network: Chain;
}>;
